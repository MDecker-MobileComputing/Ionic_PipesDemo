import { StringMitLaengePipe } from './string-mit-laenge.pipe';

describe('StringMitLaengePipe', () => {
  it('create an instance', () => {
    const pipe = new StringMitLaengePipe();
    expect(pipe).toBeTruthy();
  });
});
